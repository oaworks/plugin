// Handle the installer page - tell the page it's installed

var isInstalledNode = document.createElement('div');
isInstalledNode.id = 'oabutton-is-installed';
document.body.appendChild(isInstalledNode);